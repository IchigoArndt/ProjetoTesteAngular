import { FundoAmereloDirective } from './fundo-amerelo.directive';

describe('FundoAmereloDirective', () => {
  it('should create an instance', () => {
    const directive = new FundoAmereloDirective();
    expect(directive).toBeTruthy();
  });
});
