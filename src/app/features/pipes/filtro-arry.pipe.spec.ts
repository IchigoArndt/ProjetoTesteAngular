import { FiltroArryPipe } from './filtro-arry.pipe';

describe('FiltroArryPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroArryPipe();
    expect(pipe).toBeTruthy();
  });
});
