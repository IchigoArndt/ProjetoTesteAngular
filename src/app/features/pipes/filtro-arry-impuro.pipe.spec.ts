import { FiltroArryImpuroPipe } from './filtro-arry-impuro.pipe';

describe('FiltroArryImpuroPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroArryImpuroPipe();
    expect(pipe).toBeTruthy();
  });
});
